{% for category in categories %}
    <div>
        {{ "eventCategory.name"|trans }} : {{ category.name }}
        {{ "eventCategory.color"|trans }} : {{ category.color }}
    </div>
{% else %}
    <div>
        {{ "eventCategory.noCategory"|trans }}
    </div>
{% endfor %}