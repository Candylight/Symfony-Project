{% extends 'base.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('css/dashboard.css') }}">
{% endblock %}

{% block body %}

    {{ include('dashboard/dashboardHeader.html.twig') }}

    <div class="content-dashboard">
        <h1>{{ "dashboard.home.title"|trans }}</h1>
        <h2>{{ "dashboard.home.text"|trans }}</h2>
        <div class="map">
            <a target="_blank" class="button map" href="{{ path('homepage') }}">{{ "dashboard.buttons.map"|trans }}</a>
        </div>
        <div class="actions">
            <div class="part">
                <a class="button event" href="{{ path('list_event') }}">{{ "dashboard.buttons.events"|trans }}</a>
                <br/>
                <a class="button event_new" href="{{ path('new_event') }}">{{ "dashboard.buttons.createEvent"|trans }}</a>
            </div>
            <div class="part">
                <a class="button user" href="{{ path('userList') }}">{{ "dashboard.buttons.users"|trans }}</a>
                <br/>
                <a class="button map" href="{{ path('friendList') }}">{{ "dashboard.buttons.friendList"|trans }}</a>
            </div>
        </div>
        {% if is_granted('ROLE_ADMIN') %}
            <div class="admin">
                <a class="button" href="{{ path('listEventCategory') }}">{{ "dashboard.buttons.manageCategories"|trans }}</a>
            </div>
        {% endif %}
        <br/>
        <a class="button deconnexion" href="{{ path('fos_user_security_logout') }}"> {{ 'layout.logout'|trans({}, 'FOSUserBundle') }} </a>
    </div>

{% endblock %}

{% block javascripts %}
{% endblock %}
